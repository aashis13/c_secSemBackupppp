#include <stdio.h>
void main()
{
    char a;
    int b;
    float c;
    double d;
    printf("\n%u size of char", sizeof(a));
    printf("\n%lu size of int", sizeof(b));
    printf("\n%u size of float", sizeof(c));
    printf("\n%u size of double", sizeof(d));
}